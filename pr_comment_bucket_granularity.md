# 为什么只加 {1, 16}:bucket 粒度选择的数值依据

> 补充说明:为什么本 PR 的网格只补 `SMALL_PREFILL_BUCKETS = (1, 16)`,而不是
> 上游 vLLM 式的细粒度网格(`[1,2,4] + range(8,256,8) + …`)。三个问题依次回答:
> 为什么不加更多、分叉怎么定义/有多大、分叉为什么发生。所有数据来自 L20 实测
> (`test_profilling/prefill_gate_probe/micro_m_matrix16.py`,产物
> `results/matrix_m16_0915.log`、`results/matrix_m17_127_0916.log`、
> `results/matrix_m17_127_rel_0916.log`)。

---

## 1. 为什么只加 {1, 16},不加其他桶

本 PR 的目标是 **cache-hit 步的形状恒等回放**。命中步只调度未缓存尾部:

```
scheduled = ((N - 1) mod 16) + 1 ∈ [1, 16]        # block_size = 16
```

这个区间只有 16 种形状,对它们逐一实测"图宽度 M 与 eager 宽度是否逐位一致"后,
按数值安全性排序:

| 候选桶 | 覆盖的 eager 宽度 | 5 个 GEMM 形状的逐位一致性 | 结论 |
|---|---|---|---|
| **1** | 仅 tail=1 | 精确匹配,结构保证 | ✅ 必须加(M=1 走 gemv,与 M≥2 的 kernel 档不同) |
| **16** | tail 2–16 | **全部逐位一致**(15 尾长 × 5 形状 × 2 seed) | ✅ 一张图覆盖 15/16 种尾长 |
| 2 / 4 / 8 | tail 2 / 4 / 8 | 与 M=16 同核(同一 cuBLAS kernel) | ❌ 纯冗余:同 kernel ⇒ 数值不更安全、GEMM 时间相同(都踩在权重读取的 memory-bound 地板上) |
| 32 | 部分命中 17–32 | 逐位一致,**除 down_proj 的 ref=17**(p99 差 20%) | ⏸ 唯一值得考虑的扩展,但仅服务"部分命中" |
| 64 | 部分命中 33–64 | qkv/o/gate_up/lm_head 一致;**down_proj 对 33–63 全部分叉** | ❌ 引入新的分叉区 |
| 128 | 已存在 | 65–127 与其一致;17–64 分叉 | 现状保留(见下) |

两个加性发现,说明现状其实比预期好:

- **65–127 的部分命中已经安全**:实测该区间与 M=128 逐位一致——现有 bucket-128
  图对它们本就是数值恒等的,无需任何改动;
- **33–64 是唯一的真实盲区**:任何候选(64 图、128 图)都有显著分叉,这段维持
  回落 eager。

**为什么"暂时没有需求"也是理由**:33–64 只出现在**部分命中**(缓存未覆盖全部
前缀,如 LRU 驱逐后)时。canonical 离线负载里每个 prompt 都是完整前缀命中,
`scheduled ∈ [1,16]`,这段区间不出现。是否值得为它加桶,取决于线上是否真的
存在部分命中(取决于驱逐压力与 prompt 重叠模式)——在拿到该数据前,加桶只有
确定成本(捕获时间、显存、维护)而没有确定收益。

**设计原则**:网格由实测的形状分布与同核边界决定,而不是由上游的通用性决定。
上游必须细(prefill token 数任意,8 步长才能压住 padding);我们的命中分布是
结构化确定的,最小有效集就是 {1, 16}。若后续观察到部分命中,32 是唯一有实测
安全背书的扩展(带一个可门控的洞:down_proj ref=17)。

## 2. 数值分叉怎么定义、具体有多少

**定义**:同一组输入(相同的前 k 行)、不同的 GEMM batch 宽度 M(eager 真实宽度
vs 图捕获宽度),对"同样这 k 行"的输出做**逐位比较**(bf16 下即 sha256 级别的
exact 相等)。不一致即分叉——不看浮点容差,只看 bit。

原因:下游是 beam search + top-k。实测判决实验(docs §4.3)表明 **1 个 ulp 的
logit 差异就足以翻转 beam 排序、改变最终生成序列**。因此本仓库的判据是二值的:
要么逐位一致(可回放),要么分叉(回落 eager)。误差幅度只改变"分叉概率",
不改变"会分叉"这个事实。

**分叉量(实测,以 scheduled=17 走 bucket-128 图为例,其他 ref 同量级)**:

| 统计层 | 数值 | 含义 |
|---|---|---|
| 差异元素占比 | qkv 26.7% / o_proj 41% / gate_up 36% / down_proj 48% / lm_head 36% | 约四分之一到一半的输出元素有差异 |
| 相对误差中位数 | ~0.6% ≈ **1.5 ulp** | 一半差异元素只差最后一位——纯舍入 |
| 相对误差 p99 | **15–25% ≈ 45–65 ulp** | 每 100 个差异元素有 1 个差出 ~1/5 相对量——显著 |
| 相对误差 max | 1e3–1e5 ulp | 出现在分母接近零的元素上,无参考意义 |

即:**广度上~25–50% 元素翻动,幅度上中位数是舍入位但 p99 达 45+ ulp**。这是
"不同累加顺序"的典型指纹(见 §3),不是"不同算法"的误差结构。对 beam top-k
的敏感度而言,1.5 ulp 的中位数已在可翻转区间,故不可按"差距小"接受。

## 3. 为什么会分叉:原理与机制

### 3.1 根因:浮点加法不满足结合律 + cuBLAS 按形状选 kernel

`F.linear` 的每个输出元素是一个 K 维点积(K=2048 或 6144):

```
out[i,j] = Σ_k x[i,k] · W[j,k]
```

K 个 bf16 数相加,**不同的累加顺序产生不同的舍入序列**。而 GEMM kernel 从不
按 k 串行累加——它按形状 (M, N, K) 从 cuBLAS 的启发式/调优数据库里选一个
kernel,该 kernel 决定了 tile 划分、K 分段与部分和的归并顺序:

```
eager:   cuBLAS 按形状 (scheduled, N, K) 查表 → kernel A → 归约顺序 α
图回放:  cuBLAS 按形状 (bucket,    N, K) 查表 → kernel B → 归约顺序 β
A ≠ β  ⇒  同一数学求和走了不同舍入路径 ⇒ 低位不同 ⇒ top-k 翻转 ⇒ 序列分叉
```

同形状的归约顺序是**确定**的(实测:同形状重复调用 100% 位稳定),所以分叉只
发生在"图宽度与 eager 宽度落入不同 kernel 档"的形状组合上——这正是同核矩阵
在测量的事。

### 3.2 观测到的 kernel 档边界(三份实测数据的画像)

| 边界 | 证据 | 机制解释 |
|---|---|---|
| M=1 \| M≥2 | M=1 与 M∈[2,16] 全部分叉 | gemv 专用路径 vs GEMM tiling 路径 |
| M∈[2,16] 一个档 | 2–15 与 16 逐位一致 | tile 的 BLOCK_M 与 M 无关,这些 M 切法相同 |
| M=32 一个档 | 17–32 与 32 一致(down_proj ref=17 除外) | 同上,下一档 |
| 33–64 一个档 | 33–63 与 64 一致(除 down_proj) | 下一档,但 down_proj 例外(见下) |
| M≥65 与 128 同档 | 65–127 与 128 逐位一致 | 65 起的切法与 128 相同 |

**down_proj(6144→2048)是唯一在档内也分叉的形状**(ref=17 vs M=32;
33–63 vs M=64):它是四个投影中唯一的深矩阵,K=6144 最容易触发 **split-K**
(沿 K 切给多个 block 并行、再合并部分和),而 split-K 的合并顺序对 M 敏感。
这与 §4.2 的独立观察一致(只关 `allow_bf16_reduced_precision_reduction` 能修
4 个投影中的 3 个,down_proj 仍分叉)。

### 3.3 误差的三层结构与此机制自洽

- 大部分元素的求和路径"碰巧"舍入到同一位 → med ≈ 1.5 ulp;
- 少数元素的中间量正好跨过舍入边界,不同顺序被放大 → p99 ≈ 45–65 ulp 的长尾;
- 分母近零的元素相对误差爆炸 → max 无意义。

如果是"算法不同"(如不同的数学近似),误差不会聚集在低 ulp——**双峰分布
(舍入位 + 长尾)正是"同数学、不同顺序"的指纹**。

### 3.4 本 PR 的方案为什么有效

形状恒等在因果链第一环掐断:让图回放的 M 与 eager 的 M 落入同一 kernel 档
(1 与 16 精确匹配、2–15 与 16 实证同档),kernel A = kernel B,后续整条
链(舍入路径差异 → 低位差 → top-k 翻转 → 序列分叉)都不会发生。miss 步
从来逐位一致是同一机制(两侧 M 同为 bucket);上游 vLLM 的细网格对
"17→24 pad 回放"也是同样性质的经验保证,并非结构保证——所以本仓库在
docs 中把同核结论明确标注为"本机 torch/cuBLAS 快照的实证,升级需复测"。

---

### 附:验证 `VLLM_BATCH_INVARIANT=1` 与分叉的关系

两臂同时开启 `VLLM_BATCH_INVARIANT=1`(Triton persistent matmul 覆盖,
BLOCK 与 M 无关)时,实测两臂输出逐位一致——从反方向坐实了"kernel 按形状
选择"是分叉的充分必要条件。代价约 +30–40% 时延,故仅用于 digest 门禁场景,
不作为默认。
